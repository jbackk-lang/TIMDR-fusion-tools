numpy>=1.21
pandas>=1.3
matplotlib>=3.4
h5py>=3.1
pytest>=7.0
fastapi>=0.100
uvicorn>=0.23
python-multipart>=0.0.6

# MDSplus jest opcjonalny - potrzebny WYLACZNIE do parsers/mdsplus_parser.py
# (polaczenie z serwerem MDSplus tokamaka/stellaratora). Nie jest wymagany
# do pracy z CSV/HDF5 ani do zadnego innego modulu w tym repo.
# Nieprzetestowane w tym repo (brak dostepu do serwera MDSplus w srodowisku
# rozwojowym) - zainstaluj tylko jesli faktycznie laczysz sie z MDSplus.
# MDSplus>=7.96
